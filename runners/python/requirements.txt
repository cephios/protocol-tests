# Pinned reference SDK for the Cephios Protocol v1.0 conformance runner.
#
# The runner is a thin shim (see run.py): it installs this pinned cephios-core and invokes its
# §17.3 conformance runner (`python -m cephios_core.conformance <vectors>`) against the published
# v1.0/ vectors in this repository.
#
# Pinned to the published PyPI release (cephios-core is public on PyPI; no token / deploy key needed).
cephios-core==0.1.0
